// Deprecated en v0.6 — el bottom nav vive ahora dentro de AppShell.tsx
// Solo se mantiene este archivo para no romper imports legacy. Borrar después.
export default function BottomNav() { return null; }
