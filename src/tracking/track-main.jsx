import React from 'react'
import ReactDOM from 'react-dom/client'
import TrackingPedido from './TrackingPedido'
import './tracking.css'

ReactDOM.createRoot(document.getElementById('track-root')).render(
  <React.StrictMode>
    <TrackingPedido />
  </React.StrictMode>
)
